---
name: calligraphy-collector
description: >
  书法集字作品生成工具。输入文本内容+书体+书法家+排版格式，
  自动从书法字典网站（shufazidian.com）抓取单字真迹图片，
  按排版格式整合成集字作品 HTML 效果演示。
  触发词：集字、书法集字、集字作品、书法作品生成、
  calligraphy collection、集字创作。
display_name: "书法集字生成器"
display_name_en: "Calligraphy Collector"
description_zh: >
  书法集字作品生成工具。输入文本内容+书体+书法家+排版格式，
  自动从书法字典网站抓取单字真迹图片，整合成集字作品。
  支持行/楷/草/隶/篆/魏碑 6 种书体、横排/竖排/斗方/对联 4 种排版。
  缺字自动回退相似书体/相似字形，每字提供 3 个备选版本。
  触发词：集字、书法集字、集字作品、书法作品生成、calligraphy collection。
description_en: >
  Calligraphy artwork generator. Input text + style + calligrapher + layout,
  auto-scrapes single-character images from calligraphy dictionary sites and
  composes them into a styled HTML presentation.
visibility: "public"
agent_created: true
---

# Calligraphy Collector · 书法集字作品生成器

从 [书法字典](http://shufazidian.com)（400 万+ 字形库）自动搜取指定书法家的单字真迹，按排版格式整合成集字作品 HTML 页面。

> **核心理念**：输入「内容 + 书体 + 书法家 + 排版」，输出一个可直接展示/打印的集字作品。

## 触发条件

- 用户说"集字"、"集字作品"、"书法集字"
- 用户说"帮我生成一副书法对联/横幅/斗方"
- 用户提供文本+书体+书法家并要求排版
- 对话中出现 `/calligraphy-collector` 斜杠命令

## 快速开始

```bash
python scripts/collector.py --text "宁静致远" --font "行书" --author "王羲之" --layout "横排"
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `--text` | ✅ | — | 要集字的文本（中文） |
| `--font` | | 行书 | 书体：行书/楷书/草书/隶书/篆书/魏碑 |
| `--author` | | 王羲之 | 书法家名称 |
| `--layout` | | 横排 | 排版：横排/竖排/斗方/对联 |
| `--size` | | 200 | 单字尺寸（像素） |
| `--output` | | `output/calligraphy_collection.html` | 输出路径 |

### 例句

```bash
# 行书横幅
python scripts/collector.py --text "厚德载物" --font "行书" --author "王羲之" --layout "横排"

# 楷书对联
python scripts/collector.py --text "春风大雅能容物，秋水文章不染尘" --font "楷书" --author "颜真卿" --layout "对联"

# 隶书斗方
python scripts/collector.py --text "海纳百川" --font "隶书" --author "曹全" --layout "斗方"

# 魏碑竖排
python scripts/collector.py --text "不忘初心" --font "魏碑" --layout "竖排"
```

---

## 工作流程（4 阶段流水线）

### Phase 1：解析输入

```
用户输入 → 提取纯汉字 → 去重保持顺序
```

例：`"守正创新担使命，奋楫争先立潮头"` → `['守','正','创','新','担','使','命','奋','楫','争','先','立','潮','头']`

### Phase 2：逐字搜取

对每个汉字，Playwright 自动化操作 shufazidian.com：

```
打开搜索页 → 选择书体 → 输入汉字 → 搜索 → 滚动加载 → 解析结果
```

每条结果提取：

| 字段 | 来源 | CSS 选择器 |
|------|------|-----------|
| 书法家 | 按钮文字 | `a.btnSFJ` |
| 缩略图 | 图片链接 | `div.mbpho a img` |
| 详情链接 | 链接 href | `div.mbpho a` |

### Phase 3：智能筛选 & 回退

按优先级尝试获取每个字的图片（最多 3 个版本）：

```
┌─ 策略 1：指定书体搜索 ──────────────────────────┐
│  结果 → 按书法家名筛选 → 下载 → 校验             │
│         ✓ 够 3 个 → 结束                          │
│         ✗ 不够   → 进入策略 2                     │
└──────────────────────────────────────────────────┘
         ↓
┌─ 策略 2：回退相似书体 ──────────────────────────┐
│  魏碑→楷书  草书→行书  篆书→隶书                 │
│  结果 → 同上筛选流程 → 补足版本                    │
│         ✗ 仍不够 → 进入策略 3                     │
└──────────────────────────────────────────────────┘
         ↓
┌─ 策略 3：相似字形代替 ──────────────────────────┐
│  楫 → 揖、辑    担 → 丹、但    奋 → 夺、奔       │
│  在已尝试书体中搜索替代字 → 标注"用X代替"         │
└──────────────────────────────────────────────────┘
```

#### 图片白名单过滤

> ⚠️ shufazidian.com 返回多种类型的图片，必须严格过滤。

| URL 路径模式 | 内容类型 | 处理 |
|-------------|---------|------|
| `/gq/` | ✅ 真实单字书法图 | **接受**（唯一白名单） |
| `/shufa6/` | ❌ 字形集锦网格（多书法家对比） | 拒绝 |
| `/png/` | ❌ PNG 格式（非原始扫描） | 拒绝 |
| `image/app.png` | ❌ App 下载引导图 | 拒绝 |
| 含 `jizi`/`shici` | ❌ 集字/诗词多字图 | 拒绝 |

> **关键**：`/shufa6/` 路径的图片宽高比正常，但内容是**多书法家字形网格拼图**，不是单字。必须通过 URL 路径白名单（而非宽高比）来拦截。

#### 书法家匹配规则

根据 `AUTHOR_ALIASES` 字典做模糊匹配：

```python
"王羲之" → ["王羲之", "羲之"]
"赵孟頫" → ["赵孟頫", "孟頫", "子昂"]
"苏轼"   → ["苏轼", "东坡", "子瞻"]
```

匹配逻辑：结果中的书法家名称 **包含** 任一别名即视为命中。

> 优先级：匹配指定书法家 > 不匹配但可用的结果

### Phase 4：合成输出

1. **图片处理**：PIL 自动裁切留白 → 缩放到指定尺寸 → 对比度增强 → 居中放入宣纸色画布
2. **排版合成**：按指定格式将所有单字拼合到一张大图上
3. **加盖印章**：右下角红色「集字」印章
4. **生成 HTML**：
   - 上部分：集字作品大图
   - 下部分：单字详情卡片（每个字显示主版本 + 备选版本缩略图 + 作者信息）

---

## 排版格式详解

### 横排（Horizontal）

```
┌──────────────────────────────────────────┐
│          宁    静    致    远             │
└──────────────────────────────────────────┘
```
从左到右水平排列，适合现代横幅、牌匾。

### 竖排（Vertical）

```
┌────┐
│ 宁 │
│ 静 │
│ 致 │
│ 远 │
└────┘
```
从上到下排列，传统书法卷轴风格。

### 斗方（Square）

```
┌────┬────┐
│ 海 │ 纳 │
├────┼────┤
│ 百 │ 川 │
└────┴────┘
```
正方形网格布局，适合 4 字/9 字方形作品。

### 对联（Couplet）

```
┌────┬────┐
│ 实 │ 匠 │  上联（左）  下联（右）
│ 业 │ 心 │
│ 报 │ 铸 │
│ 国 │ 魂 │
└────┴────┘
```
左右两列竖排，上联在左、下联在右。

---

## 书体回退链

当指定书体无结果时，按以下顺序自动尝试相似书体：

| 主书体 | 回退顺序 | 理由 |
|--------|---------|------|
| 魏碑 | 楷书 → 行书 | 魏碑是楷书前身，字形最接近 |
| 草书 | 行书 → 楷书 | 草书是行书的快速写法 |
| 篆书 | 隶书 → 简牍 → 楷书 | 篆隶同源，简牍过渡 |
| 楷书 | 行书 → 魏碑 | 行书保留楷书骨架 |
| 行书 | 楷书 → 草书 | 双向可逆 |
| 隶书 | 楷书 → 行书 | 隶书笔画特征与楷书接近 |

---

## 相似字形映射

当所有书体回退仍无结果时，使用形近字代替：

| 原字 | 替代字 | 选择理由 |
|------|--------|---------|
| 楫 | 揖、辑、戢 | 同"咠"声旁，外形高度相似 |
| 担 | 丹、但 | 同"旦"声旁 |
| 奋 | 夺、奔 | 同"大"部件 |
| 潮 | 朝、嘲 | 同"朝"声旁 |
| 魂 | 魄、鬼 | 同"鬼"部件 |

> 可自行扩展 `SIMILAR_CHARS` 字典添加更多映射。

---

## 输出 HTML 结构

```
┌──────────────────────────────────┐
│          集字作品（标题）          │
│      原文 · 排版信息              │
│  ┌────────────────────────────┐  │
│  │    集字作品大图（PNG）       │  │
│  │    + 宣纸背景 + 集字印章     │  │
│  └────────────────────────────┘  │
│                                  │
│        单字详情（网格）           │
│  ┌──────┐ ┌──────┐ ┌──────┐     │
│  │ 宁   │ │ 静   │ │ 致   │ ...  │
│  │[主图]│ │[主图]│ │[主图]│     │
│  │王羲之│ │王羲之│ │王羲之│     │
│  │[备选]│ │[备选]│ │[备选]│     │
│  └──────┘ └──────┘ └──────┘     │
│                                  │
│     由 WorkBuddy 自动生成         │
└──────────────────────────────────┘
```

每个字卡片显示：
- **主版本**（120×120 像素）
- **书法家名称**
- **备选版本行**（2 个 60×60 缩略图，含各自作者）

未找到的字符用虚线框 + 原因标注。

---

## 技术架构

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  CLI 入口    │───▶│ Playwright   │───▶│ shufazidian │
│  argparse   │    │ (Chrome)     │    │   .com      │
└─────────────┘    └──────┬───────┘    └─────────────┘
                          │ 图片URL + 元数据
                          ▼
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  HTML 输出   │◀───│  PIL 合成    │◀───│ requests    │
│  base64内嵌  │    │  + 印章      │    │ 下载图片     │
└─────────────┘    └──────────────┘    └─────────────┘
```

| 组件 | 技术 | 用途 |
|------|------|------|
| 浏览器自动化 | Python Playwright (`channel="chrome"`) | 搜取 shufazidian.com 动态内容 |
| 图片下载 | `requests` + Referer 头 | 获取高清单字图片 |
| 图片处理 | `Pillow` (PIL) | 裁切留白、缩放、对比度、排版合成、印章 |
| 输出 | HTML + base64 内嵌 PNG | 单文件、可离线查看、可打印 |

### 文件结构

```
calligraphy-collector/
├── SKILL.md                  ← 本文件
└── scripts/
    └── collector.py          ← 核心脚本（530 行）
```

### 依赖

```bash
pip install playwright Pillow requests
```

> 无需安装 Playwright 浏览器 — 脚本通过 `channel="chrome"` 直接复用系统 Chrome。

---

## 性能基准

以 14 字魏碑对联 `"守正创新担使命，奋楫争先立潮头"` 为例：

| 阶段 | 耗时 | 说明 |
|------|------|------|
| 单字搜索+过滤 | 3-8 秒/字 | 含 Playwright 页面加载 + 滚动 |
| 图片下载 | 0.5-2 秒/字 | 取决于网络和图片大小 |
| 图片处理+合成 | <1 秒 | PIL 纯本地 |
| HTML 生成 | <0.5 秒 | 含 base64 编码 |
| **总计（14 字）** | **~90 秒** | 含请求间隔（防限流） |

---

## 已知限制

| 限制 | 影响 | 应对 |
|------|------|------|
| shufazidian.com 需 JS 环境 | 无法用纯 HTTP 请求抓取 | Playwright + 系统 Chrome |
| 冷门字+冷门书体组合 | 可能匹配为 0 | 自动回退相似书体 + 相似字形 |
| 相似字形字典不完整 | 未映射的字符无法代替 | 用户可自行扩展 `SIMILAR_CHARS` |
| 魏碑/简牍多为佚名 | 作者显示"未知" | 属历史事实，非 bug |
| 请求频率受限 | 过快可能被限流 | 每字间隔 1-2.5 秒 |

---

## 与同类工具对比

| 特性 | 本工具 | 以观书法 App | 不厌书法 |
|------|--------|-------------|---------|
| 命令行操作 | ✅ | ❌ | ❌ |
| 自动回退书体 | ✅ | ❌ | ❌ |
| 多版本备选 | ✅ | ✅ | ❌ |
| 相似字形代替 | ✅ | ❌ | ❌ |
| 批量集字 | ✅ | ✅ | ✅ |
| 离线使用 | HTML 输出可离线 | App 需在线 | App 需在线 |
| 自定义排版 | 横/竖/斗方/对联 | 有 | 有限 |
| 数据源 | shufazidian.com | 自有数据库 | 自有数据库 |
